<div class="grid grid-cols-1 md:grid-cols-[320px,1fr] gap-4">
  <!-- Sol panel: Playlists + create -->
  <aside class="space-y-4">
    <div class="bg-white/80 dark:bg-panel/80 p-4 rounded-xl2 shadow-glass">
      <div class="flex items-center gap-2 mb-3">
        <input id="mainSearch" placeholder="Ara: şarkı, sanatçı, YouTube" class="flex-1 px-3 py-2 rounded-full bg-black/5 dark:bg-white/8 outline-none" />
        <button id="searchBtn" class="px-3 py-2 rounded-full bg-accent text-white">Ara</button>
      </div>

      <div class="mb-2 flex items-center justify-between">
        <h3 class="font-semibold">Çalma Listeleri</h3>
        <button id="createPlBtn" class="text-xs px-2 py-1 rounded-full bg-white/10">Oluştur</button>
      </div>

      <div id="playlistsGrid" class="space-y-2"></div>

      <div class="mt-4 text-xs text-gray-500 dark:text-white/50">
        İlk kullanımda otomatik bir playlist oluşturulur. Playlist kapak resmi URL'siyle güncellenebilir.
      </div>
    </div>

    <div id="cachedPanel" class="bg-white/80 dark:bg-panel/80 p-3 rounded-xl2 shadow-glass">
      <h4 class="font-semibold mb-2">İndirilenler (Çevrimdışı)</h4>
      <div id="cachedList" class="space-y-2 text-sm text-gray-600 dark:text-white/60"></div>
    </div>
  </aside>

  <!-- Sağ panel: sonuçlar / playlist detay -->
  <section class="space-y-4">
    <div id="homeArea" class="bg-white/80 dark:bg-panel/80 p-4 rounded-xl2 shadow-glass">
      <h2 class="text-lg font-semibold mb-3">Öneriler & Sonuçlar</h2>
      <div id="results" class="space-y-2"></div>
    </div>

    <div id="playlistDetail" class="hidden bg-white/80 dark:bg-panel/80 p-4 rounded-xl2 shadow-glass">
      <div class="flex items-center gap-4 mb-3">
        <img id="detailCover" src="/icons/icon-512.png" class="w-20 h-20 rounded-lg object-cover"/>
        <div>
          <div id="detailName" class="font-semibold text-lg">Playlist</div>
          <div id="detailCount" class="text-xs text-gray-500">0 parça</div>
        </div>
      </div>
      <ul id="detailTracks" class="space-y-2"></ul>
    </div>
  </section>
</div>
